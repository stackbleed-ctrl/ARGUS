# ARGUS
the full engine. Two-layer detection: fast regex patterns catch the obvious stuff instantly, then Claude AI does semantic analysis to find what patterns miss  auth bypasses, JWT algorithm confusion, TOCTOU races, business logic flaws. Async, chunked for large files, duplicates overlapping findings. Exits with code 2 on CRITICAL CI flails loudly 
